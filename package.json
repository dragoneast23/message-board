{
  "name": "message-board",
  "version": "1.0.0",
  "description": "留言板应用 - Cloudflare Workers + D1",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute message-board-db --file schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}